name: build_py

on:
  workflow_run:
    workflows: [build_cpp]
    types: [completed]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Show environment
        run: echo "🔎 The name of your branch is ${{ github.ref }}."
