@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

include("${CMAKE_CURRENT_LIST_DIR}/navground_core_pyTargets.cmake")

# these are needed to export dependencies
# similar to ament_export_dependencies

check_required_components(navground_core_py)